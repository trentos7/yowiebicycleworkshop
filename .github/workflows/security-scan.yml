name: Security Scan

on:
  push:
  pull_request:
  schedule:
    - cron: "0 2 * * *"   # Daily

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2